## API

Apis for fetching and tracking ads

### Config

Update environment variables in `advertise.env`

```
DATABASE_URL=
API_URL=
AWS_URL=
JWT_SECRET=
REDIS_URL=
SMTP_EMAIL=
SMTP_HOST=
SMTP_PASS=
SMTP_PORT=
SMTP_USER=
```
